# Active white-label baseline

This directory contains the active migration history for clean installations.
The previous incomplete history is preserved in `alembic/versions` for forensic
reference and is intentionally excluded from Alembic's `version_locations`.
